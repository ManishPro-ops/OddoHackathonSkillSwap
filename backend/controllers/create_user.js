const {user} = require("../models/user");
const {hashpass} = require("./authsrvice");

async function create_user(req,res) {

    try {
        
        // const {name , email, password , location , skil_offerd , skill_request}


    } catch (error) {
        
    }
    
}
